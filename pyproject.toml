[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dbof-in-native-grid"
version = "0.0.dev0"
description = "Fronts in the Ocean"
readme = "README.md"
license = { text = "BSD" }
authors = [
    { name = "J. Xavier Prochaska", email = "jxp@ucsc.edu" },
    { name = "P. Cornillon"},
    { name = "J. Tallman", email = "jttallman@ucdavis.edu" }
]
classifiers = [
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Oceanography",
    "Programming Language :: Python :: 3"
]

urls = { "Homepage" = "https://github.com/AI-for-Ocean-Science/" }
requires-python = ">=3.12"

dependencies = [
    "numpy",
    "Reader",
    "aiohttp",
    "torch==2.8.0",
    "torchvision",
    "smart-open[s3]",
    "pyarrow",
    "scikit-learn",
    "scikit-image",
    "tqdm",
    "cftime",
    "umap-learn",
    "llvmlite",
    "boto3==1.41.5",
    "xarray==2025.10.1",
    "h5netcdf",
    "emcee",
    "corner",
    "importlib-metadata",
    "timm==0.3.2",
    "xmitgcm",
    "ipython",
    "dask==2025.10.0",
    "s3fs==2025.9.0",
    "ujson",
    "zarr",
    "gsw",
    "xgcm",
    "seawater",
    "ecco_v4_py",
    "scikit-fmm",
    "pyyaml"
]

[project.optional-dependencies]

plotting = [
    "matplotlib",
    "seaborn",
    "bokeh",
    "cmocean",
]

test = ["pytest"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"dbof.plotting" = ["*.yaml"]

[tool.pytest.ini_options]
markers = [
    "integration: touches real data stores (network and/or cluster filesystem)",
    "mit: requires the MIT orcd filesystem (/orcd/data/abodner/003/LLC4320)",
    "s3_dbof: requires network + NRP S3 credentials for the dbof bucket (s3-west.nrp-nautilus.io)",
    "osn: requires network access to the public OSN kerchunk store (anonymous)",
]
# Integration tests are skipped unless --run-integration is passed;
# that gating lives in tests/conftest.py (pytest_collection_modifyitems).
# Show each test's logging.info lines live (asked date -> confirmed store time).
log_cli = true
log_cli_level = "INFO"
log_cli_format = "%(levelname)s  %(message)s"

[project.scripts]
generate-llc-dataset = "dbof.cli.generate_cutout_dataset:main"
generate-global = "dbof.cli.generate_global:main"
generate-global-grid-zarr = "dbof.cli.generate_grid_global:main"
zarr-to-netcdf = "dbof.cli.zarr_to_netcdf:main"
transfer-timestep = "dbof.cli.transfer_llc4320:main"
run-all-subsets = "dbof.cli.run_all_subsets:main"
generate-tile = "dbof.cli.generate_tile:main"