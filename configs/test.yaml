run:
  run_id: "test00"
  log_dir: "../test_logs/"

data:
  sampling_step: 168
  start_record: 1180
  timestep_hours: 336

sampling:
  bias_to_high_gradients: 1.3
  sample_points_per_snapshot: 10

output:
  target_km_res: 150
  down_sample_res: 64
  s3_endpoint: "https://s3-west.nrp-nautilus.io"
  bucket: "llc/"
  folder: "native_grid_dbof_training_data/"
  dataset_name: "dataset_creation.zarr"

features:
  model_data_feature_channels:
    - Eta
    - Salt
    - Theta
    - U
    - V
    - W
  compute_features_channels:
    - log_gradb

runtime:
  zarr_async_concurrency: 128
